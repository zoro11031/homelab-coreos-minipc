# Cosign signing keys (generated by bluebuild)
cosign.key
cosign.private

# Custom Containerfile if present
/Containerfile

# Sensitive configuration files
config/wireguard/wg0.conf
config/wireguard/.env
config/wireguard/keys/
config/wireguard/peer-configs/
config/wireguard/*.key
*.key
*.pem

# Docker Compose environment files (contain secrets)
compose/.env
.env

# Log files
*.log
/var/log/

# Backup files
*.backup
*.bak
*~

# Temporary files
*.tmp
*.swp
.DS_Store

# IDE and editor files
# VS Code: Keep project settings, exclude user settings
.vscode/*.log
.vscode/settings.local.json
.vscode/.react/
.vscode-test/

# Other IDEs
.idea/
*.code-workspace

# System files
Thumbs.db
.directory

# Ignition configuration files (exclude actual configs, keep templates)
ignition/*.bu
ignition/*.ign
!ignition/*.template

# Go build artifacts
homelab-setup/bin/
homelab-setup/*.exe
homelab-setup/*.dll
homelab-setup/*.so
homelab-setup/*.dylib

# Go test artifacts
homelab-setup/*.test
homelab-setup/*.out
homelab-setup/coverage.out
homelab-setup/coverage.html
homelab-setup/.coverage

# Go vendor directory (if using vendoring)
homelab-setup/vendor/

# Dev container volumes (local development)
.devcontainer/.cache/
